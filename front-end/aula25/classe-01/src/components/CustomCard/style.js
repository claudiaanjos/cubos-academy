import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    root: {
        width: 200,
        height: 'auto',
        marginBottom: 20,
        marginTop: 50,
    },
    media: {
        height: 'auto',
        width: '100%', 
    },
    cardHeader: {
        textAlign: 'center',
    }
}));